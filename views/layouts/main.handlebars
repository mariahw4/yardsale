<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">
    <title>Yardsale</title>
</head>

<main class="bg-primary-subtle">
    <header class="bg-dark d-flex container-fluid justify-content-between" style="height: 8.1em;">
        <div class="d-inline">

            <a href="/"><img src="/images/yardsaleclear.png" alt="yardsale image"
                    style="height: 100xp; width: 125px; margin: 5px; " class="col-1"></a>
            <!-- <h1 class="bg-primary-subtle">Yard Sale</h1> -->
        </div>
        <div class="input-group my-5 mx-3 w-25">
            {{!-- search function for continued development to find listings --}}
            {{!-- <input type="text" class="form-control col-4" placeholder="Search" aria-label="Search"
                aria-describedby="button-addon2"> --}}
            {{!-- <button class="btn btn-outline-secondary" type="button" id="button-addon2">Search</button> --}}
            <a href="/profile" class="mx-3 text-decoration-none fs-3">Profile</a>
            <a href="/" class="mx-3 text-decoration-none fs-3">Home</a>
        </div>
        <div class="align-self-center mx-3 logout-login-btn fs-5" >

            {{!-- If logged in, show Log Out link. If logged out, show Log In link. --}}
            <a href="/login" {{#if loggedIn}} style="display:none" {{/if}} class="text-decoration-none">Log In</a>

            <a href='/' {{#unless loggedIn}} style="display:none" {{/unless}} class=" text-decoration-none" id="logout" >Log Out</a>
        </div>
    </header>

    <body class="bg-primary-subtle">
        {{{body}}}
    </body>

    <script src="/js/logout.js"></script>

</main>

</html>